
name: Build and Parse Secret to it

on:
  workflow_dispatch:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: run script
      run: |
        ls -la
        pwd
        ./run.sh {{ MY_REPO_SECRET }} 